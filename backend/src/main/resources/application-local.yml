spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/biodata_local
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  mail:
    host: localhost
    port: 1025
    username:
    password:

server:
  port: 8080

logging:
  level:
    root: INFO
    com.biodata: DEBUG

jwt:
  secret: local-development-secret-key-change-in-production

app:
  email:
    from: noreply@biodata-local.com

file:
  upload-dir: ./uploads/local